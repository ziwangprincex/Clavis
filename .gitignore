.DS_Store
target/
node_modules/
web/node_modules/
web/dist/
web/tsconfig.tsbuildinfo
__pycache__/
*.pyc
.autoresearch/
REVIEW.md

# TeXstudio .cwl command library — fetched at build time by
# tools/fetch-cwl.mjs, pinned via tools/cwl-version.json. Not committed: it is
# ~10 MB of GPLv3 upstream data that would otherwise churn the diff on updates.
resources/cwl/

# Claude Code / skill tooling — local environment, not project source.
.agents/
.claude/
skills-lock.json
